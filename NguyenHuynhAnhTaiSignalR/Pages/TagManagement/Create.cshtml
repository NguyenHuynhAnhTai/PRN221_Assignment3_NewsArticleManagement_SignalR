@page
@model NguyenHuynhAnhTaiSignalR.Pages.TagManagement.CreateModel

@{
    Layout = "_Layout";
}

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div class="form-group">
                <label asp-for="Tag.TagId" class="control-label"></label>
                <input asp-for="Tag.TagId" class="form-control" />
                <span asp-validation-for="Tag.TagId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Tag.TagName" class="control-label"></label>
                <input asp-for="Tag.TagName" class="form-control" />
                <span asp-validation-for="Tag.TagName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Tag.Note" class="control-label"></label>
                <input asp-for="Tag.Note" class="form-control" />
                <span asp-validation-for="Tag.Note" class="text-danger"></span>
            </div>
            @if (!string.IsNullOrEmpty(Model.Message))
            {
                <div style="color: red" id="error-message">@Model.Message</div>
            }
            <div class="form-group" style="margin-top: 2%">
                <input type="submit" value="Create" class="btn btn-primary" />
                <a asp-page="/TagManagement/Index" class="btn btn-secondary">Close</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            const errorMessage = document.getElementById("error-message");
            if (errorMessage) {
                const message = errorMessage.textContent.trim();
                if (message !== "") {
                    setTimeout(function () {
                        errorMessage.style.display = "none";
                        window.location.href = '/TagManagement/Index';
                    }, 1000);
                } else {
                    setTimeout(function () {
                        errorMessage.style.display = "none";
                    }, 1000);
                }
            }
        });
    </script>
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
